server:
  port: 9001

eureka:
  instance:
    hostname: 172.30.235.200
  client:
    serviceUrl:
      defaultZone: discovery.charles3.svc.cluster.local
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
