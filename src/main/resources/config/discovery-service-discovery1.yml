server:
  port: 8761

eureka:
  instance:
    hostname: discovery1-charles3.apps.qqd02ebq.usgovvirginia.aroapp.azure.us
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
