server:
  port: 80

eureka:
  instance:
    hostname: sample-microservice-4.charles3.svc.cluster.local
  client:
    serviceUrl:
      defaultZone: discovery.charles3.svc.cluster.local
      
logging:
  pattern: 
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
